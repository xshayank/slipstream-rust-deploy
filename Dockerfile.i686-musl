FROM messense/rust-musl-cross:i686-musl

RUN apt-get update && apt-get install -y \
    curl \
    cmake \
    pkg-config \
    perl \
    git \
    ca-certificates \
    wget \
    make \
    && rm -rf /var/lib/apt/lists/*

RUN wget https://www.openssl.org/source/openssl-3.0.13.tar.gz && \
    tar -xzf openssl-3.0.13.tar.gz && \
    cd openssl-3.0.13 && \
    CC=i686-unknown-linux-musl-gcc ./Configure linux-x86 no-shared --prefix=/usr/local/musl/i686-unknown-linux-musl && \
    make -j$(nproc) && \
    make install_sw && \
    cd .. && rm -rf openssl-3.0.13*

ENV RUST_TARGET=i686-unknown-linux-musl

WORKDIR /workspace

COPY build-docker.sh /workspace/build-docker.sh
RUN chmod +x /workspace/build-docker.sh

CMD ["/workspace/build-docker.sh"]
